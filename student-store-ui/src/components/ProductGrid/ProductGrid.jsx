import * as React from "react"
import "./ProductGrid.css"

export default function ProductGrid(props){
    return(/*Accepts the following props: products, handleAddItemToCart & remove*/
        <div className="product-grid">
        </div>
    );
}
